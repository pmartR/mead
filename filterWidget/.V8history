x.dataset = {A:1, B:2}
var x.dataset = {A:1, B:2}
x.dataset
var x.dataset = {A:1, B:2}
var dataset = {A:1, B:2}
dataset
_.keys(dataser)
_.keys(dataset)
._keys(dat)
._keys(dat)
var steve = ._keys(dat)
_.keys(dataset)
_.keys(dat)
dat
var obj = {a: 2, b: 52}
_.keys(obj)
_.keys(p)
p
_.keys(p)
var steve = _.keys(p)
steve
obj
_.keys(obj)
_.values(p)
p
